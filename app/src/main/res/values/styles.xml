<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--<style name="AppTheme" parent="@android:style/Theme.Holo.Light.NoActionBar.TranslucentDecor">-->
    <!--</style>-->
    <style name="AppTheme" parent="AppTheme.Base" />

    <style name="AppTheme.Base" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <item name="colorControlActivated">@color/colorPrimary</item>
        <item name="colorSwitchThumbNormal">@android:color/white</item>
        <item name="android:colorForeground">@color/colorPrimaryGray</item>
    </style>

    <style name="popFromBottom_animation">
        <item name="android:windowEnterAnimation">@anim/popupfrombottom_enter</item>
        <item name="android:windowExitAnimation">@anim/popupfrombottom_exit</item>
    </style>

    <style name="popFromTop_animation">
        <item name="android:windowEnterAnimation">@anim/popupfromtop_enter</item>
        <item name="android:windowExitAnimation">@anim/popupfromtop_exit</item>
    </style>

    <style name="Theme.Transparent" parent="android:Theme.Dialog">
        <item name="android:windowIsTranslucent">true</item>//此对话框的背景
        <item name="android:windowBackground">@android:color/transparent</item>//对话框是否透明
        <item name="android:windowContentOverlay">@null</item>//对话框是否有遮盖
        <item name="android:windowNoTitle">true</item>//对话框无标题
        <item name="android:windowIsFloating">true</item>
        //对话框是否浮动
        <item name="android:backgroundDimEnabled">true</item>//是否允许对话框的背景变暗
    </style>

    <style name="Theme.Intro" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>
	<style name="Dialog" parent="Theme.AppCompat.Light.Dialog">
		<item name="android:background">@android:color/transparent</item>
		<item name="android:windowBackground">@android:color/transparent</item>
		<item name="android:windowNoTitle">true</item>
	</style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>
